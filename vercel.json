{
  "buildCommand": "npm --prefix knowverse/frontend install && npm --prefix knowverse/frontend run build",
  "outputDirectory": "knowverse/frontend/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
